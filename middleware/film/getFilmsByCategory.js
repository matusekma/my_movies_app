/**
 *  Gets the films of the given categoryid
 *  if the category exists
 *  else redirects to /lists
 */
module.exports = function (objrepo) {

    return function (req, res, next) {
        return next();
    };

};