/**
 *  Checks if the given e-mail address is in the database and
 *  sends reminder e-mail
 */

module.exports = function (objrepo) {

    return function (req, res, next) {
        return next();
    };

};